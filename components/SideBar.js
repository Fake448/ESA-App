
const style = {
   background: 'linear-gradient(to right bottom, #ffffff, #0081ff)',
   height: "100%",
   width: "100px"
 }

const SideBar = props => (
   <div style={style}>
      SideBar
   </div>
 );
 
 export default SideBar;